import { DirectionClassPipe } from './direction-class.pipe';

describe('DirectionClassPipe', () => {
  it('create an instance', () => {
    const pipe = new DirectionClassPipe();
    expect(pipe).toBeTruthy();
  });
});
